if SHIELD_SPLAYBREAK_LEFT

config ZMK_KEYBOARD_NAME
    default "SPLAYBREAK"
    
config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_SPLAYBREAK_LEFT || SHIELD_SPLAYBREAK_RIGHT

config ZMK_SPLIT
    default y

endif
